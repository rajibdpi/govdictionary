name: govdictionary
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# version
version: 1.0.0+1

# environment
environment:
  sdk: '>=3.3.4 <4.0.0'

# dependencies
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.3
  http: ^1.2.1
  connectivity_plus: ^6.0.3
  badges: ^3.1.2
  provider: ^6.1.2
  shared_preferences: ^2.5.3
  url_launcher: ^6.2.3
  share_plus: ^11.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  inno_bundle: ^0.9.0
  flutter_launcher_icons:
    android: true
    ios: true
    image_path: "assets/icon/icon.png"
# For information on the generic Dart package dependencies, see
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
      - assets/words.json
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
inno_bundle:
  id: 75b4e920-117d-11f0-8036-4945daf5aa58
  publisher: Rajib Ahmed
  publisher_url: https://rajibdpi.github.io
  publisher_email: rajibdpi@gmail.com
  name: Govt Dictionary
  version: 1.0.0
  copyright: Copyright © 2025
  url: https://github.com/rajibdpi/govdictionary